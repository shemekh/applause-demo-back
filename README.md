# Applause Demo Backend application

### Steps to run this application
1. Make sure you have installed and configured following:
    1. Java
    1. Maven
1. In your command line execute:
```
mvn spring-boot:run
```